import { BuildConfig } from 'vite'

const config: BuildConfig = {
  base: '',
  assetsDir: 'static',
  // entry: '',
  outDir: './docs',
  // assetsInlineLimit: {},
  // cssCodeSplit: {}
}

export default config
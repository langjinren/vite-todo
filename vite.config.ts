import { BuildConfig } from 'vite'

const config: BuildConfig = {
  base: '',
  assetsDir: 'static',
  // entry: '',
  // outDir: '',
  // assetsInlineLimit: {},
  // cssCodeSplit: {}
}

export default config